# git 에 프로젝트를 올리는 방법

1 .gitignore 파일 생성

2 로컬 저장소 초기화
  git init
3 파일 스테이징 및 커밋
  git add .
  git commit -m "initial commit"
4 GitHub 리포지토리(Repository) 생성
  생성 후 https 주소를 복사 :
  예) https://github.com/molopark/my-python-app.git
5 원격 저장소 연결 및 푸시(Push)
  git remote add origin https://github.com/molopark/my-python-app.git
  git branch -M main
  git push -u origin main
